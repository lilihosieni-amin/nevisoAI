-- Set charset and collation
SET NAMES utf8mb4;
SET character_set_client = utf8mb4;

-- Create database if not exists (backup will use this)
CREATE DATABASE IF NOT EXISTS neviso_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE neviso_db;
